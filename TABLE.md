# 전체 용어 한눈에 보기 · Master Table

> AI 용어집 바이블의 **전체 101개 항목**을 한 장에. 한 줄 요약만 담았습니다 — 정의·비유·흔한 오해·출처는 각 용어의 카테고리 파일을 보세요.

> 이 표는 `terms/`에서 자동 추출되어 본문과 항상 동기화됩니다(수기 편집 금지).


---


## 1. LLM 기초 · LLM Basics  ·  [자세히](terms/01-llm-basics.md)

| 용어 | 한 줄 요약 |
|---|---|
| **Token · 토큰** | 모델이 글을 읽고 쓸 때 쓰는 최소 단위. 글자도 단어도 아닌, 그 중간쯤의 조각이다. |
| **Tokenizer · 토크나이저** | 사람의 글을 모델이 읽을 수 있는 토큰 조각으로 자르는 규칙이자 그 프로그램. |
| **Context window · 컨텍스트 윈도우** | 모델이 답을 만들 때 한 번에 "볼 수 있는" 텍스트의 총량. 모델의 작업 기억이다. |
| **Embedding · 임베딩** | 단어나 문장의 "뜻"을 숫자 좌표로 바꾼 것. 뜻이 비슷하면 좌표도 가깝다. |
| **Hallucination · 환각(할루시네이션)** | 모델이 사실이 아닌 내용을, 사실인 것처럼 그럴듯하게 지어내는 현상. |
| **Inference · 추론(실행단계)** | 학습이 끝난 모델을 실제로 써서, 입력을 넣고 출력을 받아내는 단계. |
| **Parameter / Weight · 파라미터·가중치** | 모델 안에 든 수백억 개의 조절 손잡이. 학습이란 이 손잡이들을 돌려 맞추는 일이다. |
| **Temperature · 온도** | 답을 고를 때 "주사위를 얼마나 세게 흔드느냐"를 정하는 값. 높으면 뜻밖의 말, 낮으면 무난한 말. |
| **Multimodal · 멀티모달** | 글만이 아니라 이미지·음성·영상까지 한 모델이 함께 다루는 것. 눈·귀·입을 여럿 가진 셈이다. |
| **Knowledge cutoff · 지식 컷오프(학습 컷오프)** | 모델의 지식이 멈춰 있는 시점. 그 이후에 벌어진 일은 (검색·도구 없이는) 모른다. |
| **LLM · 대규모 언어 모델 (Large Language Model) / Foundation model · 파운데이션 모델** | 방대한 글로 훈련해 "다음에 올 말"을 예측하며 언어를 이해·생성하는 거대한 신경망. 이렇게 넓게 훈련돼 온갖 일에 갖다 쓸 수 있는 범용 모델을 파운데이션 모델이라 부르고, LLM은 그 대표 사례다. |
| **Transformer / Attention · 트랜스포머 / 어텐션** | 오늘날 거의 모든 LLM의 뼈대가 되는 설계. 그 핵심은 문장 속 단어들이 서로 얼마나 관련되는지 "주의(attention)"를 나눠 매기며 문맥을 읽는 방식이다. |
| **Generative AI · 생성형 AI (GenAI)** | 배운 패턴을 바탕으로 글·이미지·음성·코드 같은 "새 콘텐츠"를 만들어 내는 AI의 총칭. 분류·예측만 하던 전통 AI와 대비된다. |
| **Pretraining · 사전학습** | 특정 과제를 가르치기 전에, 방대한 미분류 데이터로 모델에 언어·표현의 기초 체력을 먼저 길러 두는 단계. |
| **Diffusion model · 디퓨전 모델** | 뿌연 노이즈에서 시작해 조금씩 노이즈를 걷어내며 이미지·영상을 만들어 내는 모델. 미드저니·Stable Diffusion·Sora류가 이 원리다. |

## 2. 프롬프트 & 상호작용 · Prompting & Interaction  ·  [자세히](terms/02-prompting.md)

| 용어 | 한 줄 요약 |
|---|---|
| **Prompt · 프롬프트** | 모델에게 답을 끌어내려고 건네는 자연어 요청 — 질문·지시가 적힌 쪽지다. |
| **System prompt · 시스템 프롬프트** | 대화 앞단에서 모델의 역할·어조·경계를 미리 정해두는 지시문. |
| **Few-shot / In-context learning · 퓨샷·인컨텍스트 러닝** | 가중치를 건드리지 않고, 프롬프트 안에 예시 몇 개를 보여줘 그 자리에서 과제를 배우게 하는 방식. |
| **Chain-of-thought · 생각의 사슬(CoT)** | 답만 내놓지 말고 중간 풀이 과정을 단계별로 쓰게 해, 복잡한 추론의 정확도를 높이는 방식. |
| **Reasoning / Thinking mode · 추론 모드** | 모델이 최종 답을 내놓기 전에, 속으로 더 길게 '생각'하는 단계를 거치도록 만든 작동 방식. |
| **Context engineering · 컨텍스트 엔지니어링** | 프롬프트 한 줄을 넘어, 모델의 컨텍스트 윈도우에 무엇을 어떻게 채워 넣을지를 통째로 설계하는 일. |
| **Prompt injection / Jailbreak · 프롬프트 인젝션 / 탈옥** | 인젝션은 데이터에 숨은 지시로 모델을 '탈취'하는 공격이고, 탈옥은 안전장치를 '우회'해 금지된 답을 끌어내는 것 — 자주 혼동되나 서로 다르다. |

## 3. AI 서비스 구축 · Building with AI  ·  [자세히](terms/03-building.md)

| 용어 | 한 줄 요약 |
|---|---|
| **RAG · 검색 증강 생성 (Retrieval-Augmented Generation)** | AI가 답하기 전에 외부 문서를 검색해, 그 내용을 근거로 답을 만들게 하는 방식. |
| **MCP · 모델 컨텍스트 프로토콜 (Model Context Protocol)** | AI 앱을 외부 데이터·도구에 연결하는 개방형 표준. 표준 단자 하나로 무엇이든 꽂는다. |
| **Agent · 에이전트 (AI Agent)** | 목표만 주면 스스로 필요한 도구를 골라, 여러 단계를 거쳐 일을 완수하는 시스템. |
| **Tool use / Function calling · 도구 사용 · 함수 호출** | 모델이 필요하다고 판단하면 계산기·검색 같은 외부 도구를 스스로 불러 쓰게 하는 기능. |
| **Vector DB / Embedding search · 벡터DB · 임베딩 검색** | 뜻이 비슷한 데이터끼리 가까이 저장해두고, 질문과 의미가 가까운 것을 근처에서 빠르게 찾아주는 검색. |
| **Embedding model · 임베딩 모델** | 문장·문서 같은 데이터를 '의미 좌표'인 임베딩 벡터로 바꿔주는 전용 모델. 답을 쓰는 생성 모델과는 역할이 다르다. |
| **Chunking · 청킹** | 긴 문서를 검색·임베딩하기 좋게 작은 조각으로 잘라두는 전처리 작업. |
| **Reranking · 리랭킹(리랭커)** | 1차 검색으로 대충 추린 후보 문서들을, 더 정밀한 모델로 질문과의 관련도를 다시 매겨 상위만 남기는 2단계 정제. |
| **Structured output / JSON mode · 구조화 출력** | 모델이 자유로운 문장이 아니라, 미리 정한 **틀(예: JSON 스키마)**에 딱 맞춰 답하도록 강제하는 기능. |
| **Harness · 하네스 (에이전트 하네스 / 평가 하네스)** | 모델을 감싸 도구·권한·컨텍스트·종료 조건을 정하는 실행 껍데기. 같은 모델이라도 하네스가 다르면 결과가 달라진다. |
| **Agent memory · 에이전트 메모리** | 에이전트가 대화·작업이 끝나도 정보를 저장해 두었다가 다시 꺼내 써, 여러 세션에 걸쳐 맥락을 이어 가게 하는 것. |

## 4. 모델 커스터마이징 (파인튜닝) · Model Customization (Fine-Tuning)  ·  [자세히](terms/04-finetuning.md)

| 용어 | 한 줄 요약 |
|---|---|
| **Base model vs Instruct model · 베이스 모델과 인스트럭트 모델** | 사전학습만 끝난 모델과, 지시를 따르도록 추가로 다듬어진 모델. 파인튜닝 출발점의 첫 갈림길이다. |
| **Training / Validation / Test split · 학습·검증·테스트 분리** | 배운 데이터로 채점하지 않기 위해 데이터를 세 몫으로 나누는 것. 파인튜닝 결과를 믿을 수 있게 만드는 최소 장치다. |
| **Full fine-tuning · 전체 파인튜닝** | 모델의 모든 가중치를 다시 학습하는 방식. PEFT가 피하려는 바로 그 비용이다. |
| **PEFT · 파라미터 효율 파인튜닝 (Parameter-Efficient Fine-Tuning)** | 모델 전체를 다시 학습하는 대신, 아주 일부 파라미터만 손봐서 같은 효과를 훨씬 싸게 얻는 방법들의 총칭. |
| **SFT · 지도 파인튜닝 (Supervised Fine-Tuning)** | "이렇게 답하라"는 모범 답안 묶음을 보여 주며 모델을 다시 가르치는, 파인튜닝의 가장 기본 형태. |
| **LoRA · 로라·저계수 적응 (Low-Rank Adaptation)** | 원본 모델은 통째로 얼려 두고, 각 층에 끼운 작은 행렬만 학습해 저비용으로 모델을 맞춤화한다. |
| **QLoRA · 큐로라 (Quantized LoRA)** | 원본 모델을 4비트로 압축해 얼려 두고, 그 위에 LoRA 어댑터만 학습해 단일 GPU로도 큰 모델을 파인튜닝한다. |
| **Prefix Tuning · 프리픽스 튜닝** | 모델 전체는 얼려 두고, 모든 층의 어텐션 앞에 붙이는 학습된 '길잡이 벡터'만 훈련한다. |
| **Adapter Tuning · 어댑터 튜닝** | 원본 모델은 얼려 두고, 층과 층 사이에 끼운 작은 '변환 부품'만 학습한다. |
| **Instruction Tuning · 인스트럭션 튜닝** | 다양한 지시-응답 예시로 모델을 통째로 다시 가르쳐, "지시를 따르는 법" 자체를 익히게 하는 완전 미세조정이다. |
| **P-Tuning · 피튜닝 (v1/v2)** | 입력 앞에 붙는 '학습된 힌트(연속 프롬프트)'로 모델을 맞추는 기법. v1은 주로 입력층, v2는 전 층에 힌트를 넣는다. |
| **BitFit · 비트핏** | 큰 모델은 그대로 얼려 두고, 이미 있는 편향(bias) 항만 미세조정한다. |
| **Soft Prompts · Prompt Tuning · 소프트 프롬프트** | 사람이 읽을 수 없는, 학습된 벡터를 입력 앞에 붙여 모델을 유도한다. 모델이 얼려 있어도 태스크에 맞출 수 있다. |
| **Multi-Task Fine-Tuning · 멀티태스크 미세조정** | 여러 과제를 한꺼번에 학습시켜 태스크끼리 서로 도움받게 하는 미세조정 방식. |
| **Federated Fine-Tuning · 연합 미세조정** | 원본 데이터는 각자 기기에 남겨 둔 채, 로컬에서 학습한 업데이트(요약노트)만 모아 하나의 모델로 합친다. |
| **Overfitting · 과적합** | 모델이 학습 데이터를 **외우다시피** 지나치게 맞춰진 나머지, 정작 처음 보는 새 데이터에서는 성능이 떨어지는 현상. |
| **Catastrophic forgetting · 파국적 망각** | 새 과제를 가르치면 이전에 잘하던 것을 급격히 잊어버리는 신경망의 고질적 성질. |

## 5. 정렬 · 강화학습 · Alignment & Reinforcement Learning  ·  [자세히](terms/05-alignment-rl.md)

| 용어 | 한 줄 요약 |
|---|---|
| **RLHF · 인간 피드백 기반 강화학습 (Reinforcement Learning from Human Feedback)** | 사람이 여러 답에 순위를 매겨 "코치(보상모델)"를 학습시키고, 그 코치를 심판 삼아 모델을 훈련하는 방식. |
| **RLAIF · AI 피드백 기반 강화학습 (Reinforcement Learning from AI Feedback)** | 답에 순위를 매기는 "코치" 역할을 사람 대신 다른 AI가 맡는 RLHF. |
| **DPO · 직접 선호 최적화 (Direct Preference Optimization)** | 코치(보상모델)도 강화학습 루프도 없이, "이게 더 낫다"는 선호쌍만으로 모델을 곧바로 교정하는 방식. |
| **GRPO · 그룹 상대 정책 최적화 (Group Relative Policy Optimization)** | 한 문제에 여러 답을 내게 하고, 별도 심판(critic) 없이 그 조(組) 안에서 상대적으로 나은 답 쪽으로 학습하는 강화학습. |
| **Alignment · 정렬 (AI Alignment)** | AI가 **똑똑하기만** 한 게 아니라 **우리가 원하는 대로, 안전하게** 행동하도록 사람의 의도·가치에 맞춰 두는 것. 이 장의 RLHF·RLAIF·DPO·GRPO가 그 구체적 방법들이다. |
| **사례: OpenPipe ART / RULER — 수작업 보상 설계를 건너뛰는 에이전트 RL** | — |

## 6. 바이브코딩 워크플로우 · Vibe-Coding Workflow  ·  [자세히](terms/06-vibe-coding.md)

| 용어 | 한 줄 요약 |
|---|---|
| **Vendoring · 벤더링** | 외부 라이브러리의 소스 복사본을 내 프로젝트 폴더 안에 넣어두고, 그대로 함께 관리하는 것. |
| **Porting · 포팅** | 한 환경(OS·CPU·언어·프레임워크)에서 돌던 프로그램을 다른 환경에서도 돌게 고쳐 옮기는 일. |
| **Refactoring · 리팩터링** | 겉으로 드러나는 동작은 그대로 두고, 내부 구조만 이해·수정하기 쉽게 다듬는 것. |
| **Scaffolding · 스캐폴딩** | 명령 한 번으로 기본 뼈대 파일·보일러플레이트를 자동 생성해, 개발의 출발점을 깔아주는 것. |
| **Wrapping · 래핑** | 기존 코드를 바깥 껍데기로 감싸, 원본은 그대로 둔 채 다른 인터페이스처럼 쓰거나 더 쉽게 쓰게 하는 것. |
| **Modularization · 모듈화** | 큰 프로그램을 책임(관심사)별 독립 모듈로 쪼개고, 정해진 인터페이스로만 서로 소통하게 하는 것. |

## 7. 개발 단계 · 품질 · Development Stages & Quality  ·  [자세히](terms/07-dev-stages.md)

| 용어 | 한 줄 요약 |
|---|---|
| **MVP · 최소 기능 제품 (Minimum Viable Product)** | 가장 적은 노력으로 "사람들이 이걸 원하나"를 배우게 해주는, 출시 가능한 최소한의 제품. |
| **PoC · 개념 증명 (Proof of Concept)** | "이게 기술적으로 되긴 하나?"만 딱 확인하는, 버리기 위해 만드는 소규모 검증 실험. |
| **Production Ready · 프로덕션 레디 (Production Readiness)** | "내 컴퓨터에선 잘 돌아요"를 넘어, 실제 사용자 트래픽에 안전하게 올릴 수 있는 상태. |
| **Evals · 평가·벤치마크 (Evaluation & Benchmarks)** | 모델·시스템이 얼마나 잘하는지를 정해진 과제와 지표로 재보는 것 — "이 모델이 좋다"는 말의 근거를 읽는 법. |

## 8. 모델 포맷·경량화 · Model Formats & Compression  ·  [자세히](terms/08-model-formats.md)

| 용어 | 한 줄 요약 |
|---|---|
| **Quantization · 양자화** | 모델 가중치를 표현하는 숫자의 비트 수를 줄여(예: 16→4비트) 크기와 메모리를 낮추는 압축 기법. |
| **GGUF** | llama.cpp 생태계에서 (주로 양자화된) 모델을 담아 배포·로딩하는 단일 파일 포맷. |
| **MLX** | 애플 실리콘(M1·M2 등)에 최적화된, 로컬에서 학습·추론을 돌리는 머신러닝 프레임워크. |
| **safetensors** | 텐서(모델 가중치)를 **안전하고 빠르게** 저장하는 파일 포맷. 코드 실행 위험이 있는 pickle을 대체한다. |
| **MoE · 전문가 혼합(Mixture of Experts)** | 입력마다 전체가 아니라 **일부 전문가 서브네트워크만** 켜서 계산해, 큰 용량을 적은 연산으로 쓰는 구조. |
| **Pruning · 프루닝(가지치기)** | 중요도가 낮은 연결을 잘라내 모델을 가볍게 만드는 경량화. 양자화·증류와 나란한 세 번째 갈래다. |
| **SLM · 소형 언어 모델 (Small Language Model)** | 기기 안에서 돌릴 수 있을 만큼 작은 언어 모델. 크기 기준에 합의된 정의가 없다는 점이 이 용어의 핵심이다. |
| **Active parameters · 활성 파라미터** | 토큰 하나를 처리할 때 실제로 쓰이는 파라미터 수. 전체 파라미터와 달라서, 모델 크기를 하나의 숫자로 말할 수 없게 만든다. |
| **Distillation · 증류(지식 증류)** | 크고 똑똑한 '교사' 모델의 지식을 작고 가벼운 '학생' 모델로 옮겨, 작은 모델의 성능을 끌어올리는 압축 기법. |

## 9. 로컬 실행 · 셀프호스팅 · Running Models Locally  ·  [자세히](terms/09-local-run.md)

| 용어 | 한 줄 요약 |
|---|---|
| **VRAM · 지피유 전용 메모리 (Video RAM)** | GPU에 붙은 전용 메모리. 로컬에서 무엇을 돌릴 수 있고 무엇을 못 돌리는지를 사실상 혼자 정한다. |
| **llama.cpp · 라마씨피피** | 노트북·데스크톱 같은 평범한 하드웨어에서도 LLM을 돌릴 수 있게 만든 C/C++ 경량 추론 엔진. |
| **Ollama · 올라마** | 명령어 한 줄로 오픈 모델을 받아 로컬에서 실행하게 해주는 도구. |
| **LM Studio · 엘엠 스튜디오** | 로컬에서 LLM을 받아 실행하고, OpenAI 호환 API 서버까지 띄워주는 GUI 앱. |
| **Self-hosting · 셀프호스팅** | 외부 클라우드에 맡기지 않고, 내 서버·PC에서 직접 소프트웨어·모델·서비스를 운영하는 것. |
| **Open weights vs Open source · 오픈 웨이트 vs 오픈소스** | 가중치를 내려받아 쓸 수 있는 것(오픈 웨이트)과, 사용·연구·수정·재배포가 자유롭고 학습 데이터 정보·코드까지 공개된 것(오픈소스)은 다르다 — "가중치 공개 ≠ 오픈소스". |

## 10. 개발·인프라 기초 · Dev & Infra Basics  ·  [자세히](terms/10-dev-infra.md)

| 용어 | 한 줄 요약 |
|---|---|
| **SSH · 시큐어 셸 (Secure Shell)** | 멀리 있는 컴퓨터에 암호로 잠긴 안전한 통로를 뚫고 접속해, 내 컴퓨터 앞에 앉은 것처럼 명령을 내리는 방식. |
| **CLI · 커맨드라인 인터페이스 (Command-Line Interface)** | 버튼을 누르는 대신, 글자로 된 명령을 타이핑해서 프로그램을 다루는 방식. |
| **cron · 크론** | "매일 새벽 3시에 이 작업 실행"처럼, 정해둔 시각에 컴퓨터가 알아서 작업을 자동 실행하게 해주는 예약 장치. |
| **Docker / Container · 컨테이너** | 프로그램과 그게 돌아가는 데 필요한 모든 것을 하나의 밀봉 상자로 묶어, 어느 컴퓨터에서든 똑같이 실행되게 하는 방식. |
| **API · 응용 프로그램 인터페이스 (Application Programming Interface)** | 프로그램끼리 "이렇게 요청하면 이런 답을 준다"는 약속으로 소통하는 창구. |
| **Port / localhost · 포트/로컬호스트** | 한 컴퓨터 안에서 여러 서비스를 구분하는 번호(포트)와, "바로 이 컴퓨터 자신"을 가리키는 주소(localhost). |
| **Environment variable · 환경변수 (.env)** | 코드 바깥에서 프로그램에 설정값·비밀키를 넣어주는 "값 주머니". 흔히 `.env` 파일에 적어둔다. |

## 11. 버전 관리·협업 · Version Control & Collaboration  ·  [자세히](terms/11-version-control.md)

| 용어 | 한 줄 요약 |
|---|---|
| **CommonMark / GFM · 마크다운 표준과 GitHub 확장** | 마크다운에는 표준(CommonMark)이 있고 GitHub 확장(GFM)이 있다. 렌더러마다 결과가 달라지는 이유가 여기 있다. |
| **Git vs GitHub · 깃 vs 깃허브** | Git은 변경 이력을 관리하는 "도구"이고, GitHub은 그 결과물을 인터넷에 올려 남과 함께 쓰는 "장소"다 — 둘은 다른 것이다. |
| **Repository (repo) · 리포지터리(저장소)** | 프로젝트의 파일 전체와 그동안의 모든 변경 이력을 함께 담아두는 저장 단위(=프로젝트 폴더 + 그 역사). |
| **Commit · 커밋** | 지금까지 고친 내용을 "이 시점"으로 이력에 남기는 저장 지점 — 설명 메시지를 붙인 스냅샷(사진 한 장). |
| **Branch / Merge · 브랜치·머지** | 브랜치 = 본류를 건드리지 않고 갈래를 따로 내 작업하는 것; 머지 = 그 갈래를 다시 본류에 합치는 것. |
| **Clone / Pull / Push · 클론·풀·푸시** | 원격 저장소와 내 것을 맞추는 3동작 — 클론(처음 통째 복제) · 풀(원격의 최신 변경 받아오기) · 푸시(내 커밋 올려보내기). |
| **Fork · 포크** | 남의 저장소를 내 계정으로 통째 복제해, 원본과 별개로 내 마음대로 고칠 수 있게 만드는 것(주로 GitHub 등 플랫폼 기능). |
| **Pull request (PR) · 풀 리퀘스트** | 내가 고친 변경을 원본에 "이렇게 바꿨는데 반영해 주실래요?" 하고 제안해, 리뷰·논의를 거쳐 합치는 절차(GitHub 등 플랫폼 기능). |

## 12. 안전·거버넌스 · Safety & Governance  ·  [자세히](terms/12-safety-governance.md)

| 용어 | 한 줄 요약 |
|---|---|
| **Guardrails · 가드레일** | AI가 하면 안 되는 말·형식을 벗어난 답을 내놓지 못하게, 입력과 출력 앞뒤에 세워둔 안전 울타리. |
| **Red-teaming · 레드팀** | 배포 전에 일부러 AI를 공격하고 꾀어내, 나쁜 답·탈옥·편향 같은 약점을 먼저 찾아내는 모의 침투 시험. |
| **Model card · 모델 카드** | 이 AI 모델이 뭘 하고, 어디까지 잘하고 어디서 못하는지를 정해진 서식으로 적어둔 '제품 설명서 겸 성분표'. |
| **Watermarking · 워터마킹** | AI가 만든 글·이미지임을 나중에 알아볼 수 있게, 사람 눈엔 안 보이는 통계적 표식을 출력에 몰래 심는 기법. |
| **EU AI Act 위험등급 · EU AI 법 위험 분류** | 유럽연합의 AI 법이 AI를 위험도에 따라 4단계로 나누고(+ 범용 AI는 별도), 단계가 높을수록 더 무거운 의무를 지우는 규제 틀. |

---


_총 101개 항목 · 자동 생성_
